package apis.endpoints;

public final class JsonEndpoint {

    private JsonEndpoint()
    {

    }
    private static final String USER_DIRECTORY = System.getProperty("user.dir");

    public static final String CONTRACTS_DIRECTORY = "contracts";
}
